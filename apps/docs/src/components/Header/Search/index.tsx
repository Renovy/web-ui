import { Button } from "@renovy/web-ui";

export function Search() {
  return (
    <Button.Root className="w-full lg:w-48">
      <Button.Content className="text-start w-full">Procurar...</Button.Content>
    </Button.Root>
  );
}
