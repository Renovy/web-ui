{
  "name": "web-ui",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "pnpm run --filter @renovy/web-ui build",
    "publish": "pnpm run --filter @renovy/web-ui publish",
    "dev": "pnpm run --parallel --filter @renovy/web-ui --filter examples \"/^dev.*/\""
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "@renovy/web-ui": "workspace:^"
  }
}
