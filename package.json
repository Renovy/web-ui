{
  "name": "web-ui",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "pnpm run --filter @renovy/web-ui build",
    "build-docs": "pnpm run --filter docs build",
    "publish": "pnpm run --filter @renovy/web-ui publish",
    "dev": "pnpm run --parallel --filter @renovy/web-ui --filter docs \"/^dev.*/\""
  },
  "keywords": [],
  "author": "",
  "workspaces": [
    "apps/docs",
    "packages/web-ui"
  ]
}
